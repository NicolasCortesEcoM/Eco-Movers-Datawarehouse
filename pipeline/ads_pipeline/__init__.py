"""Ad-platform extraction (Phase C). One client per platform, all shaped like
sm_pipeline: credentials from .env, every call in the shared ledger, a per-session
budget, and a dlt resource with a composite PK and merge disposition.

See marketing_ads_integration_guide.md before touching anything here.
"""
